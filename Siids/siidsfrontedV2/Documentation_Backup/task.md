# Git Backup and Synchronization Task List

- `[/]` Create analysis document for frontendV2.
- `[ ]` Copy critical artifacts to the backup folder.
- `[ ]` Stage files using `git add .`.
- `[ ]` Stash files using `git stash push`.
- `[ ]` Checkout the `dev` branch.
- `[ ]` Pull latest changes from `origin dev`.
